1) OS
    CREATE TABLE "ORDENS_SERVICOS" 
   (	"ID_OS" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"NUM_OS" VARCHAR2(4000) NOT NULL ENABLE, 
	"DAT_SOLICITACAO" DATE NOT NULL ENABLE, 
	"SOLICITANTE" VARCHAR2(20 CHAR) NOT NULL ENABLE, 
	"ASSUNTO" VARCHAR2(4000 CHAR) NOT NULL ENABLE, 
	"OBS" VARCHAR2(4000 CHAR), 
	"CATEGORIA" VARCHAR2(4000) NOT NULL ENABLE, 
	"SETOR" VARCHAR2(100), 
	 CONSTRAINT "ORDENS_SERVICOS_PK" PRIMARY KEY ("ID_OS")
  USING INDEX  ENABLE
   ) ;

2) IMG OS
  CREATE TABLE "IMG_ORDENS_SERVICOS" 
   (	"NUM_IMG" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"FK_ORDENS_SERVICOS" NUMBER NOT NULL ENABLE, 
	"ARQ" BLOB NOT NULL ENABLE, 
	"MIMETYPE" VARCHAR2(200 CHAR) NOT NULL ENABLE, 
	"FILENAME" VARCHAR2(200 CHAR) NOT NULL ENABLE, 
	 CONSTRAINT "IMG_ORDENS_SERVICOS_PK" PRIMARY KEY ("NUM_IMG")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "IMG_ORDENS_SERVICOS" ADD CONSTRAINT "IMG_ORDENS_SERVICOS_CON" FOREIGN KEY ("FK_ORDENS_SERVICOS")
	  REFERENCES "ORDENS_SERVICOS" ("ID_OS") ENABLE;

3) TMP OS
  CREATE TABLE "TMP_ORDENS_SERVICOS" 
   (	"NUM_IMG" NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"CPF_SOLICITANTE" VARCHAR2(20 CHAR) NOT NULL ENABLE, 
	"ARQ" BLOB NOT NULL ENABLE, 
	"MIMETYPE" VARCHAR2(200 CHAR) NOT NULL ENABLE, 
	"FILENAME" VARCHAR2(200 CHAR) NOT NULL ENABLE, 
	 CONSTRAINT "TMP_ORDENS_SERVICOS_PK" PRIMARY KEY ("NUM_IMG")
  USING INDEX  ENABLE
   ) ;